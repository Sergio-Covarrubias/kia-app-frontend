export type UserType = {
    id: number;
};

export type SigninDataType = {
    corporateId: string;
    password: string;
    firstName: string;
    middleName: string;
    lastName: string;
}

export type LoginDataType = {
    corporateId: string;
    password: string;
};
